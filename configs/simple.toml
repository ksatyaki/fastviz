# fastviz config — generated by the topic discoverer.
reference_frame = "map"

[map]
topics = ["/esdf_costmap"]

[poses]
topics = ["/goal_pose"]

[pose_arrays]
topics = ["/guide_poses"]

[paths]
topics = ["/coverage_plan", "/planned_path"]

